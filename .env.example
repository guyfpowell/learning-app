EXPO_PUBLIC_API_URL=http://localhost:3000/api

# Feature flags
# Set to true only when running a custom dev build (not Expo Go)
EXPO_PUBLIC_STRIPE_ENABLED=false
# Set to true to enable hosted Stripe checkout flow
EXPO_PUBLIC_STRIPE_CHECKOUT_ENABLED=false
